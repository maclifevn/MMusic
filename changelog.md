# Changelog

## 1.0.1

- Built-in ad-blocking enabled by default.
- Download tracks as audio files (mp3 256kbps by default), saved to `~/Downloads`.
- Startup and memory optimizations: lazy-loaded heavy features, per-language
  translations, deferred plugin loading, spellcheck disabled.
- Signed with a Developer ID certificate and notarized on macOS.
